using UnityEngine;

public class TankAttack : MonsterAttack
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public override void attack() {
        Debug.Log("tank attack");
    }
}
